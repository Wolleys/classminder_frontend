import Title from "../../../../components/page-title";
import { Divider } from "@mui/material";

const Courses = () => {
    return (
        <>
            <Title title="Courses" />
            <Divider sx={{mb: 3}} />
        </>
    );
};

export default Courses;
